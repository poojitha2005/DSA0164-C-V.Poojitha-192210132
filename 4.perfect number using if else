#include<iostream>
using namespace std;
int main()
{
	int num,i,sum=0;
	cout<<"enter the number:";
	cin>>num;
	for(i=1;i<num;i++)
	{
		if(num%i==0)
		{
			sum=sum+i;
		}
	}
	if(sum==num)
	{
		cout<<"perfect number";
	}
	else
	{
		cout<<"not perfect number";
	}
	return 0;
}
