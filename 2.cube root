#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int num;
	cout<<"enter the number:";
	cin>>num;
	if(num<0)
	{
		cout<<"cube root of negative number is not real:";
	}
	else
	{
		cout<<"cube root of number:"<<cbrt(num);
	}
	return 0;
}
